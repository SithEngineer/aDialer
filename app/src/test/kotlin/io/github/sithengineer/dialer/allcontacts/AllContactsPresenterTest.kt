package io.github.sithengineer.dialer.allcontacts

import org.jetbrains.spek.api.Spek
import org.jetbrains.spek.api.dsl.given
import org.jetbrains.spek.api.dsl.it
import org.jetbrains.spek.api.dsl.on
import org.jetbrains.spek.api.dsl.xit

class AllContactsPresenterTest : Spek({
  given("a presenter for listing all contacts") {
    on("all contacts view created") {
      xit("should have presenter set") {

      }
    }

    on("presenter started") {
      xit("should request all loaded contacts") {

      }
    }

    on("contact selected") {
      xit("should make a call to the selected contact") {

      }
    }
  }
})